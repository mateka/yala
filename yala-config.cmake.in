@PACKAGE_INIT@

#get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
#include(${SELF_DIR}/mylibTargets.cmake)

#set_and_check(FOO_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

#find_dependency()

include(${CMAKE_CURRENT_LIST_DIR}/yala-targets.cmake)

check_required_components(yala)
